# Codex 开发交互记录

## 任务输入

用户提供了“数据结构项目.zip”和“李正丹-朱静雯老师的题目.docx”，要求先阅读题目、核对是否需要 Docker、检查未完成需求、完成项目并给出启动方法。

## 主要交互与实现决策

| 阶段 | 用户/问题 | Codex 执行与结果 |
|---|---|---|
| 需求分析 | 阅读教师题目并判断 Docker 是否必需 | 提取出 5 组验收要求；确认只要求部署 Open WebUI 与至少一个大模型，Docker 不是硬性要求。 |
| 数据整理 | 检查压缩包内历年数据结构题 | 整理 2009–2025 年 218 道题，按年份、章节、考点、难度和题型结构化；客观题 184 道。 |
| 初版实现 | 建知识库、提示词和扩展 | 创建分类 Markdown、课程助教系统提示词和含 5 个函数的 Workspace Tool；添加单元测试与数据校验。 |
| 运行环境纠正 | 用户要求仔细确认本机 Docker 与未完成需求 | 重新检查后发现 Docker Desktop 已安装；因容器拉取网络不稳定，改用 Windows 原生 Open WebUI，不再把 Docker 写成必需条件。 |
| 本地部署 | 连接可实际推理的本地模型 | 安装 Open WebUI 0.11.3，下载中文 BGE；依次测试 Qwen2 0.5B、Qwen2.5 1.5B，因回答质量不达标升级到 Qwen2.5 3B。 |
| RAG 调优 | 概念问题命中无关真题、工具结果与真题混淆 | 开启混合检索并定制 RAG 模板；建立“完整档案库 + 教学核心库”两层结构；新增关键辨析卡。 |
| 工具验证 | 统计、抽题、批改是否真调用 | Open WebUI 实际执行 `chapter_statistics`、`random_questions`、`grade_objective_answer` 等函数；来源元数据与工具返回文档写入验收 JSON。 |
| 验收 | 运行至少 15 项并做前后对比 | 编写 API 验收脚本，顺序执行 15 项和 5 项基线/优化对比；保存原始输出并修复工具边界，最终 15/15，基线 0/5、优化版 5/5。 |
| 交付 | 用户要求最后直接说明启动方式 | 新增原生安装、启动、停止、自动初始化和验收脚本；更新 README、部署手册、项目报告、需求清单和 Git 包。 |

## 典型失败—修复记录

1. 0.5B 模型把队列同时说成 FIFO 和 LIFO：升级模型。
2. 1.5B 模型复制无关真题：定制 RAG 模板、启用混合检索并升级 3B。
3. 3B 批改 2009-s01 时混入递归题：将 17 份真题从默认检索面移至完整档案层，精确题号改走工具。
4. 堆排序稳定性答反、BFS 标记时机含糊：新增关键辨析卡并重新入库。
5. 并发测试产生上下文串题风险：验收改为顺序执行，并固定 `seed=2026` 提高复现性。
6. 工具异常结果偶尔未被最终回复完整复述：增加 `status=completed`、`has_results=false`、`message_to_user`，并规定无结果时不得再次调用；定向复测通过。
7. Windows PowerShell 5.1 把无 BOM 的中文脚本误读后出现解析错误：将 `.ps1` 中的运行期消息改为 ASCII，并用 Windows PowerShell 原样完成停止、启动和健康检查。
8. 两个回答使用“遍历找到”“请提供相关信息”等同义表达，旧正则误判：保留实际输出，仅扩展语义等价检查词，并在验收 JSON 中记录重新判定与定向复测历史。
9. Open WebUI 报告请求约 6111 tokens、可用上下文仅 4096：确认 llama-server 将 `--ctx-size 8192` 分给两个并行槽；改为单槽 16384，并以 6201-token 实际请求和 Open WebUI 对话复测通过。
10. 上下文扩容后简单问候仍长时间只显示闪烁光标：从已保存聊天和模型日志确认请求成功但 6093-token 预处理耗时约 116 秒；定位到基础模型默认注入的通用内置工具，关闭后浏览器等价请求缩减到 30 tokens，0.76 秒返回，并将课程助教设为默认模型。

## 证据位置

- 实际对话结构化结果：`test-results/acceptance-results.json`
- 可读验收报告：`test-results/acceptance-test-report.md`
- 系统提示词：`prompts/system-prompt.md`
- 工具源码与测试：`openwebui-tools/`、`tests/`
- Git 提交历史：项目 `.git` 与交付的 history bundle

本文件是开发交互摘要，未伪造截图或不存在的模型输出；如课程要求完整聊天截图，可从 Codex 客户端另行导出本任务界面。
